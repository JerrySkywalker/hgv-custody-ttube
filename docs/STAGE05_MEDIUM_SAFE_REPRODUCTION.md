# Stage05 Medium-Safe Native Reproduction

Created: 2026-05-05

Profile: `medium_safe`

Grid:

- `h_grid_km = [800 1000]`
- `i_grid_deg = [40 50 60 70]`
- `P_grid = [2 4 6]`
- `T_grid = [2 3 4]`
- `F_fixed = 1`
- designs: `72`
- `caseId = N01`
- `trajectoryBackend = native_vtc`

Artifacts generated by the integration test:

- search result
- normalized result table
- summary
- frontier
- Pareto/transition
- plot bundle

Outputs are written to a temporary directory and are not committed.

Legacy comparison remains limited to guarded tiny/module oracle; old full Stage05 is not run.

## Final Validation Summary

`tests/integration/test_stage05MediumSafeReproduction.m` passed.

Observed reproduction scope:

- designs evaluated: `72`
- output bundle: search result, CSV result table, JSON summary, run index, frontier artifact, Pareto/transition artifact, and plot bundle
- output policy: generated under a temporary directory during tests; no generated artifact or figure was committed
- legacy comparison: not attempted for the full medium-safe grid because the guarded oracle is intentionally limited to tiny/module-level comparison

This medium-safe run demonstrates that the native Stage05 full wrapper can execute beyond the tiny parity grid while remaining below the configured guard.
